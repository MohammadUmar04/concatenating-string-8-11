var maxage = prompt(`enter your max age`);
var currentAge = prompt(`enter your currentage`);

if( currentAge && maxage && parseInt(currentAge) <= parseInt(currentAge)){
    alert( "you are welcome");
}
else{
    alert( `sorry you are not welcome`);
}

